<configuration>
    <!-- Console Appender for debugging -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OpenTelemetry Appender -->
    <appender name="OPEN_TELEMETRY" class="io.opentelemetry.instrumentation.logback.v1_0.OpenTelemetryAppender">
        <!-- Use OpenTelemetry's global SDK -->
    </appender>

    <!-- Root Logger Configuration -->
    <root level="info">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="OPEN_TELEMETRY" />
    </root>
</configuration>
